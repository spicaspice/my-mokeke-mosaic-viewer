# Exclude large/binary assets (allow images for Pages deploy)
# images/  # uncomment to exclude locally; keep committed for GitHub Pages

# Backups/archives
*.zip
*.7z
*.tar
*.tar.gz

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Node/npm cache (if ever used)
node_modules/
dist/
build/
